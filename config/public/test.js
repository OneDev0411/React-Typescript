import env from './ci'

/**
 * this environment is used for running unit tests. For now, almost no
 * environmental configuration is used in unit tests, and we are using
 * ci environment as is, but we can set any configuration here in future.
 */
export default env
