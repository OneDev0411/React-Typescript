image: node:10

cache:
  key: "$CI_BUILD_REF_NAME"
  paths:
    - node_modules/

stages:
  - build
  - test

deps:
  stage: build
  script:
    - npm i

unit:
  stage: test
  script:
    - npm run test:unit
  dependencies:
    - deps
