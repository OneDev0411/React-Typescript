image: node:10

variables:
  # It's auto-generated by some git pre-commit hook in order to keep node_modules cached
  # based on package-lock.json file checksum
  CI_CACHE_KEY: "000bf1f38524689a97e8218d8ed4b945"

stages:
  - build
  - test
  - deploy

build_deps:
  stage: build
  cache:
    key: ${CI_CACHE_KEY}
    paths:
      - node_modules/
    policy: push
  only:
    changes:
      - package-lock.json
  script:
    - npm i

test_unit:
  stage: test
  cache:
    key: ${CI_CACHE_KEY}
    paths:
      - node_modules/
    policy: pull
  script:
    # Fallback npm i command in case of cache failure.
    # It won't take much if the cache is working as expected.
    - npm i
    - npm run test:unit -- --coverage

deploy_alpha:
  stage: deploy
  image: ruby:latest
  before_script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
  script:
    - dpl --provider=heroku --app=rechat-web-alpha --api-key=$HEROKU_API_KEY
  environment:
    name: irish
    url: https://irish.rechat.com
  only:
  - alpha

deploy_irish:
  stage: deploy
  image: ruby:latest
  before_script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
  script:
    - dpl --provider=heroku --app=rechat-irish --api-key=$HEROKU_API_KEY
  environment:
    name: irish
    url: https://irish.rechat.com
  only:
  - stage

deploy_production:
  stage: deploy
  image: ruby:latest
  before_script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
  script:
    - dpl --provider=heroku --app=rechat-web --api-key=$HEROKU_API_KEY
  environment:
    name: irish
    url: https://irish.rechat.com
  when: manual
  only:
  - master
