import styled from 'styled-components'

export const Thumbnail = styled.img`
  width: 100%;
  height: auto;
  padding: 0.5rem;
  transition: all 0.5s;

  &:hover {
    background: #eee;
    cursor: pointer;
  }
`
