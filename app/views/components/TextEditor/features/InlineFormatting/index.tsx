import React from 'react'

import { ToolbarFragment } from '../../components/ToolbarFragment'

export function InlineFormattingFeature() {
  // const { getEditorState } = useContext(textEditorContext)

  // const richButtonsPlugin = useMemo(() => createRichButtonsPlugin(), [])

  return (
    <>
      <ToolbarFragment group="test">btn 1</ToolbarFragment>
    </>
  )
}
