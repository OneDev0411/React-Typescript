import { Table } from '@app/views/components/Grid/Table'

function ConnectedInstagramAccounts() {
  // TODO: implement the list
  const columns = []
  const rows = []

  return <Table columns={columns} rows={rows} totalRows={rows.length} />
}

export default ConnectedInstagramAccounts
