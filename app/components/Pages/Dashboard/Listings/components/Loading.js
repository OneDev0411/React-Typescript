import React from 'react'

const Loading = ({ text }) => (
  <div className="c-loading--text">
    <div className="c-loading--text__inner">Loading {text} Listings...</div>
  </div>
)

export default Loading
