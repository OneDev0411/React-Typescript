import styled from 'styled-components'

export const AccountInfoWrapper = styled.div`
  line-height: 1.5;
  padding-top: 10px;
  min-width: 240px;
  .header {
    display: flex;
  }
  .secondary {
    display: flex;
    padding-left: 28px;
  }
  .secondary .settings {
    position: relative;
    right: -10px;
  }
`
