import styled from 'styled-components'

export const TeamsSearch = styled.div`
  padding: 1rem 0;

  background: inherit;
  position: sticky;
  z-index: 1;
  top: -40px;

  input {
    height: 40px;
  }

  button {
    line-height: 0;
  }
`
