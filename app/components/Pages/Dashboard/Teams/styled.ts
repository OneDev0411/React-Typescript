import styled from 'styled-components'

export const TeamsSearch = styled.div`
  padding: 1rem 0;

  background: inherit;
  position: sticky;
  z-index: 1;
  top: -2.5rem;

  input {
    height: 2.5rem;
  }

  button {
    line-height: 0;
  }
`
