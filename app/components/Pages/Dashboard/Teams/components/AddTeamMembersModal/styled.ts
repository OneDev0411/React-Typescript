import styled from 'styled-components'

import OriginalButton from 'components/Button/ActionButton'

export const Button = styled(OriginalButton)`
  min-width: 6.3rem;
  justify-content: center;
`

export const RolesLabel = styled.label`
  margin: 0 2rem 0 0;
  vertical-align: top;
`
