import type { SuperCampaignDetailTabType } from './types'

export const superCampaignDetailTabs: Record<
  SuperCampaignDetailTabType,
  SuperCampaignDetailTabType
> = {
  Overview: 'Overview',
  Results: 'Results'
}
